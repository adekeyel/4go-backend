# --- Database (Railway Postgres) ---
DATABASE_URL=postgresql://postgres:UzdosdIbOOUilztvrjaaLFMtsGwqfyhS@iriguchi.proxy.rlwy.net:20537/railway

# --- Auth ---
# Generate with: openssl rand -base64 48
JWT_ACCESS_SECRET="replace-me luse"
JWT_REFRESH_SECRET="replace-me-too luse"
JWT_ACCESS_TTL="15m"
JWT_REFRESH_TTL="180d"

# --- Server ---
PORT=4000
NODE_ENV="production"
# Comma-separated list of allowed origins for CORS + Socket.io
CLIENT_ORIGINS="https://4go.com.ng,https://,https://4go-front.vercel.app/"
SITE_URL="https://4go-front.vercel.app/"

# --- Cloudinary (replaces Supabase Storage) ---
CLOUDINARY_CLOUD_NAME=""
CLOUDINARY_API_KEY=""
CLOUDINARY_API_SECRET=""

# --- Resend (transactional email, replaces the Lovable email hook) ---
RESEND_API_KEY=""
EMAIL_FROM="4go <notify@notify.4go.com.ng>"

# --- Flutterwave (payments) ---
FLUTTERWAVE_SECRET_KEY=""
FLUTTERWAVE_PUBLIC_KEY=""

# --- Push notifications (web push, replaces Supabase's push edge function) ---
VAPID_PUBLIC_KEY=""
VAPID_PRIVATE_KEY=""
VAPID_SUBJECT="mailto:support@4go.com.ng"
